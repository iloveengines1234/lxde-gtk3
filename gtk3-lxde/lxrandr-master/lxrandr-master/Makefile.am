ACLOCAL_AMFLAGS = -I .

SUBDIRS = src po data

# Modern Fix: Ensure standard GNU administration variables and compilation scripts
# are packed into the release tarball alongside configuration setups.
EXTRA_DIST = \
	autogen.sh \
	$(NULL)